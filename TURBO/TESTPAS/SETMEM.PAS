begin
   writeln(setmem(1));
   writeln(setmem(1));
   writeln(setmem(1));
   writeln(setmem(1));
   writeln(setmem(1));
end.
